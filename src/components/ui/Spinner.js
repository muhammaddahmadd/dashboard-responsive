import React from "react";
import "./Spinner.css"; // Import the CSS file with the loading spinner styles

const Spinner = () => {
  return <div className="loading-spinner"></div>;
};

export default Spinner;
